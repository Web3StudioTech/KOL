'use client'
export default function PointsPage() {
  return (
    <div style={{padding:'2rem',color:'var(--text)'}}>
      <h1 style={{fontFamily:'Bebas Neue,sans-serif',fontSize:'48px',letterSpacing:'2px',color:'var(--accent)'}}>Points System</h1>
      <p style={{color:'var(--muted)'}}>Points configuration panel — coming soon.</p>
    </div>
  )
}
